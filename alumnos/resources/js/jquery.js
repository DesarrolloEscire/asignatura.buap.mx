window.Popper = require('popper.js').default;
window.$ = window.jQuery = require('jquery');

require('bootstrap');

/**
 * Library for select2 - The jQuery replacement for select boxes
 */
 require('select2');
